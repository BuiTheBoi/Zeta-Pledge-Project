#include "plant.hpp"

#include <vector>
using namespace std;

int main() {
    vector<Plant> mySaves[3] = {
        Plant("DEFAULT", );
        }

    // View the screen (Start and options)
    

    // If options, take user input

    // If start, choose which save (plant) to use to water plants


    Plant myPlant("Cactus", 10, 10, 10, "PM");

    
    return 0;
}